/**
 * Diagnostics section re-exported for backwards compatibility.
 */

export {
  type DiagnosticsCallbacks,
  type DiagnosticsViewInput,
  renderDiagnosticsPage as renderDiagnostics,
  renderExportResult,
} from "./pages/diagnostics";
